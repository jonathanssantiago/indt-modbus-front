import DeviceReadingsList from '@/components/DeviceReadingsList';

export default function ReadingsPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <DeviceReadingsList />
    </div>
  );
}
